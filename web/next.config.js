/** @type {import('next').NextConfig} */
 nextConfig = {}

module.exports = nextConfig ={
    images:{
        domains: [
            'avatars.githubusercontent.com' , '10.0.0.117',
        ],
    }
}
